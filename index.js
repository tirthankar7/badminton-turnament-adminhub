import Layout from '../components/Layout';

const Home = () => {
    return (
        <Layout>
            <h2>Welcome to the Badminton Tournament Manager</h2>
        </Layout>
    );
};

export default Home;
